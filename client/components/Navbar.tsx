import { Navbar } from '@mantine/core';
import React from 'react';

export const NavBar = () => {
  return (
    <Navbar width={{ base: 100 }} height={500} p="xs">
      Side items
    </Navbar>
  );
};
