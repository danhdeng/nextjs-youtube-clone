import { Navbar } from '@mantine/core';
import React from 'react';

export const NavBar = () => {
  return (
    <Navbar width={{ base: 300 }} height={500} p="xs">
      Side items
    </Navbar>
  );
};
